//----Importacion para React-Boostrap-----------------------
import Button from "react-bootstrap/Button";

const Boton = (props) => {
    return <Button variant="primary">{props.nombre}</Button>;
};

export default Boton;
